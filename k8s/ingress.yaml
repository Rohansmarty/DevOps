apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-java-app-ingress
spec:
  rules:
    - host: devops-java-app.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: devops-java-app-service
                port:
                  number: 80
